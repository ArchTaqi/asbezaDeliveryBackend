{% extends 'base.html.twig' %}

{% block title %}Registrationtype{% endblock %}

{% block body %}
    <h1>Registrationtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registrationtype.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ registrationtype.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ registrationtype.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('registrationtype_index') }}">back to list</a>

    <a href="{{ path('registrationtype_edit', {'id': registrationtype.id}) }}">edit</a>

    {{ include('registrationtype/_delete_form.html.twig') }}
{% endblock %}
