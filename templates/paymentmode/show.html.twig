{% extends 'base.html.twig' %}

{% block title %}Paymentmode{% endblock %}

{% block body %}
    <h1>Paymentmode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paymentmode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paymentmode.name }}</td>
            </tr>
            <tr>
                <th>Descriiption</th>
                <td>{{ paymentmode.descriiption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paymentmode_index') }}">back to list</a>

    <a href="{{ path('paymentmode_edit', {'id': paymentmode.id}) }}">edit</a>

    {{ include('paymentmode/_delete_form.html.twig') }}
{% endblock %}
