{% extends 'base.html.twig' %}

{% block title %}Edit Branchuser{% endblock %}

{% block body %}
    <h1>Edit Branchuser</h1>

    {{ include('branchuser/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('branchuser_index') }}">back to list</a>

    {{ include('branchuser/_delete_form.html.twig') }}
{% endblock %}
