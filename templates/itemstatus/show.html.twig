{% extends 'base.html.twig' %}

{% block title %}Itemstatus{% endblock %}

{% block body %}
    <h1>Itemstatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ itemstatus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ itemstatus.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ itemstatus.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('itemstatus_index') }}">back to list</a>

    <a href="{{ path('itemstatus_edit', {'id': itemstatus.id}) }}">edit</a>

    {{ include('itemstatus/_delete_form.html.twig') }}
{% endblock %}
