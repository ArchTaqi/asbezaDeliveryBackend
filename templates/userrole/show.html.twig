{% extends 'base.html.twig' %}

{% block title %}Userrole{% endblock %}

{% block body %}
    <h1>Userrole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userrole.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ userrole.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ userrole.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('userrole_index') }}">back to list</a>

    <a href="{{ path('userrole_edit', {'id': userrole.id}) }}">edit</a>

    {{ include('userrole/_delete_form.html.twig') }}
{% endblock %}
