{% extends 'base.html.twig' %}

{% block title %}Salesperson{% endblock %}

{% block body %}
    <h1>Salesperson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salesperson.id }}</td>
            </tr>
            <tr>
                <th>Agreement</th>
                <td>{{ salesperson.agreement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salesperson_index') }}">back to list</a>

    <a href="{{ path('salesperson_edit', {'id': salesperson.id}) }}">edit</a>

    {{ include('salesperson/_delete_form.html.twig') }}
{% endblock %}
