{% extends 'base.html.twig' %}

{% block title %}Paymentstatus{% endblock %}

{% block body %}
    <h1>Paymentstatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paymentstatus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paymentstatus.name }}</td>
            </tr>
            <tr>
                <th>Descriiption</th>
                <td>{{ paymentstatus.descriiption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paymentstatus_index') }}">back to list</a>

    <a href="{{ path('paymentstatus_edit', {'id': paymentstatus.id}) }}">edit</a>

    {{ include('paymentstatus/_delete_form.html.twig') }}
{% endblock %}
