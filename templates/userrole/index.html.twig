{% extends 'base.html.twig' %}

{% block title %}Userrole index{% endblock %}

{% block body %}
    <h1>Userrole index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for userrole in userroles %}
            <tr>
                <td>{{ userrole.id }}</td>
                <td>{{ userrole.name }}</td>
                <td>{{ userrole.description }}</td>
                <td>
                    <a href="{{ path('userrole_show', {'id': userrole.id}) }}">show</a>
                    <a href="{{ path('userrole_edit', {'id': userrole.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('userrole_new') }}">Create new</a>
{% endblock %}
